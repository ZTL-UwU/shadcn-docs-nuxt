---
title: Nuxt 内容
description: 配置 Nuxt Content 模块.
icon: lucide:book
---

::Alert
---
to: https://content.nuxt.com/get-started/configuration
target: _blank
---
请查阅 **Nuxt Content** 文档中的详细配置指南.
::

默认的 Nuxt 内容配置可以在 `nuxt.config.ts` 中被覆盖.

```ts [nuxt.config.ts]
export default defineNuxtConfig({
  devtools: { enabled: true },
  extends: ['shadcn-docs-nuxt'],

  // 在这里编写
  content: {
    // ...
  },

  compatibilityDate: '2024-07-06',
});
```

## 语法高亮

### 主题

```ts [nuxt.config.ts]
export default defineNuxtConfig({
  content: {
    highlight: {
      theme: {
        // 默认主题
        default: 'github-light',
        // 深色模式使用的主题
        dark: 'github-dark',
      }
    }
  }
});
```

:read-more{to="https://shiki.style/themes"}

### 语言

默认突出显示的语言是: `['json', 'js', 'ts', 'html', 'css', 'vue', 'diff', 'shell', 'markdown', 'yaml', 'bash', 'ini']`{lang="js"}.

如果你需要突出显示其他语言, 请在你的 `nuxt.config.ts` 中添加它们.

```ts [nuxt.config.ts]
export default defineNuxtConfig({
  content: {
    highlight: {
      langs: ['c', 'cpp'],
    },
  },
});
```

:read-more{to="https://shiki.style/languages"}

## 默认配置

```ts [nuxt.config.ts]
export default defineNuxtConfig({
  content: {
    documentDriven: true,
    highlight: {
      theme: {
        default: 'github-light',
        dark: 'github-dark',
      },
      preload: ['json', 'js', 'ts', 'html', 'css', 'vue', 'diff', 'shell', 'markdown', 'mdc', 'yaml', 'bash', 'ini', 'dotenv'],
    },
    navigation: {
      fields: ['icon'],
    },
    experimental: {
      search: {
        indexed: true,
      },
    },
  },
});
```
